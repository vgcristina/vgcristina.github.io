---
import { Image } from 'astro:assets'

import SocialIcons from './SocialIcons.astro'


const {fullName, title, institute, profilePicture} = Astro.props
---
<div class='flex-1'>
  <h1 class='text-4xl font-bold mb-4'>
    ¡Bienvenido/a a mi portfolio!
  </h1>
  <p class='text-xl text-gray-600 mb-4'>{title}{institute ? ` at ${institute}` : ''}</p>
  <p class='text-lg mb-6'>
    Soy <span class='text-secondary'>{fullName}</span>, desarrolladora de software con enfoque en aplicaciones móviles y web.
    Recientemente finalicé mis estudios en desarrollo de software y actualmente estoy trabajando en CineAtmos, una app móvil para descubrir y gestionar películas y series.
    Me encanta explorar nuevas herramientas para seguir aprendiendo y mejorando cada día.
  </p>
  <div class='flex gap-4'>
    <SocialIcons />
  </div>
</div>
<div class='size-72 lg:block hidden'>
  <!-- Add your profile image -->
  <Image
    src={profilePicture}
    alt={fullName}
    class='w-full h-full mask mask-circle size-44'
  />
</div>
